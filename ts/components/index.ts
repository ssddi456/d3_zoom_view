import * as scroll_row from './scroll_row';
import * as character_card from './character_card';
import * as editable_text from './editable_text';
import * as tags from './tags';
import * as snippets from './snippets';
import * as select_menu from './select_menu';
import * as story_dag from './story_dag';
import * as dialog from './dialog';

export const components = {
    scroll_row,
    character_card,
    editable_text,
    tags,
    snippets,
    select_menu,
    dialog,
};

export const bindingPlaceHolder = {
    story_dag
};
